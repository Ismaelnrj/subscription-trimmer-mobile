# Metro bundler (React Native / Expo) requires all transitive packages to be
# reachable at node_modules/<pkg>. pnpm's default symlink layout keeps
# transitive deps in .pnpm/ and Metro cannot find them.
# Using the hoisted linker makes pnpm write a flat node_modules tree like npm.
node-linker=hoisted
